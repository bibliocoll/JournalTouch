/*!
 * Do your own changes for a kiosk that are not covered by GUI settings.
 *
 * This file won't be overwritten on updates.
 */

$(document).ready(function() {
    /*!
     * your actions go here
     */
     //alert($('#client_IP').text());       // The IP
     //alert($('#client_Agent').text());    // The browser agent string

    // EXAMPLE
    /*
    // Hide meta elements individually for a single IP
    if ($('#client_IP').text() == '1.1.1.1') {
        $('.meta_rss').hide();
        $('.meta_toc').hide();
        $('.meta_library').hide();
        $('.meta_website').hide();
        $('.meta_print').hide();
    }
    */

});
